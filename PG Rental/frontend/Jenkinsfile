pipeline {
    agent any

    stages {

        stage('Show Files') {
            steps {
                sh 'ls'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Successful'
            }
        }

    }
}
